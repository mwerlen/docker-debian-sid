A simple Debian docker used for testing deb packages
===================================================

You need to create a base.tgz with :
```
sudo pbuilder --create --distribution sid
```

Then copy it to <repo>/pbuilder/base.tgz
